# Task 3.1b-reads: Apply READ Tier Rate Limiting to All GET Routes

## Summary
Applied Tier 1 (READ — 100 req/min) rate limiting to all 10 read-only/GET API routes in the IC-OS v7.2 compliance dashboard.

## Routes Modified

### With authGuard (9 routes) — Pattern: `applyRateLimit(auth, request, 'READ')`
1. `src/app/api/dashboard/route.ts` — GET
2. `src/app/api/compliance/route.ts` — GET
3. `src/app/api/analytics/aggregate/route.ts` — GET
4. `src/app/api/analytics/fraud-ring/route.ts` — GET
5. `src/app/api/ubo-tree/route.ts` — GET
6. `src/app/api/sanctions-exceptions/route.ts` — GET
7. `src/app/api/audit-log/route.ts` — GET
8. `src/app/api/cbuae-submission-checker/route.ts` — GET
9. `src/app/api/investigation/context/route.ts` — GET

### Without authGuard (1 route) — IP-based fallback
10. `src/app/api/health/route.ts` — GET
    - Uses `checkRateLimit(getClientIdentifier(request), RATE_LIMIT_TIERS.READ)`
    - Added `request: NextRequest` parameter to function signature

## Changes Per File
- Added `import { applyRateLimit } from '@/lib/rate-limit';` (or `checkRateLimit`, `getClientIdentifier`, `RATE_LIMIT_TIERS` for health)
- Added rate limit check immediately after authGuard authorization check, before business logic
- No changes to Zod schemas, response structures, or business logic

## Lint
- 0 errors, 1 pre-existing warning (unrelated: TanStack Virtual incompatible library)
