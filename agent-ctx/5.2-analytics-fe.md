# Task 5.2-analytics-fe: Update AdvancedAnalytics.tsx and CommandCenter.tsx to use live data

## Summary
Wired both AdvancedAnalytics and CommandCenter components to live API data, replacing hardcoded mock data and toast-only buttons.

## Changes Made

### AdvancedAnalytics.tsx
- Replaced 5 hardcoded mock data arrays with computed data from `useAnalyticsAggregate()` hook
- Added `Loader2` import and loading state spinner
- Added `useAnalyticsAggregate` import from `@/lib/query-hooks`
- StatCards now show live data: Total Alerts, Open Alerts, Pending KYC, AI Queries, Avg Latency
- `handleRefresh` calls `refetchAnalytics()`
- `handleExport` uses dynamic import of `@/lib/csv-export` to generate CSV from transaction trends

### CommandCenter.tsx
- "File SAR Report" → `useICOSStore.getState().setActiveSection('goaml-filing')`
- "Run Sanctions Screen" → `useICOSStore.getState().setActiveSection('adverse-media')`
- "Start Inspection" → `useICOSStore.getState().setActiveSection('evidence-war-room')`
- "Generate Audit Pack" → fetches `/api/audits` + dynamic import `csv-export` + `generateAuditPackCSV` + `downloadCSV`

## Lint
0 errors, 0 warnings
