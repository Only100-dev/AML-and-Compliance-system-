# Task 7.1.2 — React Optimization Specialist

## Task
Add React.memo, useMemo, useCallback optimizations to UnifiedWorkspace.tsx and useInvestigationContext.ts

## Changes Made

### UnifiedWorkspace.tsx
1. **React.memo wrappers** on 5 sub-components:
   - `Customer360Profile`
   - `TransactionTimeline`
   - `AIRationalePanel`
   - `ActionPanel`
   - `CollapsibleSection`
   - Pattern: `const X = React.memo(function X(...) { ... })` — preserves displayName

2. **useCallback for handleRefresh**: Added `useCallback(() => refetch(), [refetch])` to prevent re-creating the inline arrow on every render. Applied to Refresh button's onClick.

3. **useMemo for graphPositions**: Extracted SVG node/edge position calculations from inline JSX into a memoized `graphPositions` object with `nodePositions[]` and `edgePositions[]` arrays. Depends on `[fundFlowNodes]`.

4. **Verified** existing useCallback/useMemo:
   - `handleConfirm` deps `[selectedAction, justification, onAction]` — correct
   - `handleAction` deps `[alertId, currentUser.id, currentUser.name, actionMutation]` — correct
   - `timelineEvents` deps `[alert, transactions, goamlFilings, sarCase, complianceCase]` — correct
   - `fundFlowNodes` deps `[alert, transactions]` — correct

### useInvestigationContext.ts
1. **placeholderData: keepPreviousData**: Added to useQuery options to prevent loading flicker when switching between alerts
2. Imported `keepPreviousData` from `@tanstack/react-query`
3. Verified `useInvestigationAction` onSuccess invalidation covers all relevant query keys

## Lint
0 errors, 0 new warnings
