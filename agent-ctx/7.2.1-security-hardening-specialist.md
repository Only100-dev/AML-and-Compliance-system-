# Task 7.2.1 — Rate Limiting on External Integration Adapters

## Summary
Implemented rate limiting on all external integration adapters to prevent API quota exhaustion and denial-of-wallet attacks.

## Files Created
- `/home/z/my-project/src/lib/integrations/rate-limiter.ts` — New integration-specific rate limiter module

## Files Modified
- `/home/z/my-project/src/lib/integrations/identity-provider.ts` — Added rate limit checks to 3 functions
- `/home/z/my-project/src/lib/integrations/screening-provider.ts` — Added rate limit checks to 2 functions
- `/home/z/my-project/src/lib/integrations/regulatory-gateway.ts` — Added rate limit checks to 2 functions

## Rate Limit Profiles
| Service | Limit | Window | Key Prefix |
|---------|-------|--------|------------|
| Identity (UAE Pass/Nafath) | 10 req/min | 60s | integration:identity-provider |
| Screening (Dow Jones/Refinitiv) | 20 req/min | 60s | integration:screening-provider |
| Regulatory (goAML) | 5 req/min | 60s | integration:regulatory-gateway |

## Behavior on Rate Limit
- **Identity Provider**: throws `Error('Rate limit exceeded for identity provider. Please try again later.')`
- **Screening Provider**: returns fail-closed result with `highestScore=100`, `status='ERROR'`, `failClosed=true`
- **Regulatory Gateway**: returns `SUBMISSION_FAILED` with `errorMessage` and `retryCount=0`

## Lint Result
0 errors, 1 pre-existing warning (unrelated)
