# Task 4.1b — Wire IndividualKYCWizard.tsx to Real API

**Agent**: Frontend Wiring Agent
**Status**: ✅ Completed

## Changes Made

**File Modified**: `src/components/ic-os/kyc/IndividualKYCWizard.tsx`

### 1. Added Imports (line 51-53)
- `useCreateIndividualKYC`, `ApiMutationError` from `@/lib/query-hooks`
- `Loader2` from `lucide-react`
- `toast` from `sonner`

### 2. Added Mutation Hook (line 308)
- `const createIndKYCMutation = useCreateIndividualKYC();` inside the component

### 3. Replaced handleSubmit (lines 344-394)
- Changed from synchronous local-state-only function to async API-calling function
- Added validation check with `toast.error` for missing required fields
- Calls `createIndKYCMutation.mutate()` with payload: fullName, emiratesId, passportNo, nationality, isPep, riskRating, eddRequired
- On success: shows `toast.success` with result.message, resets form, switches to profiles tab
- On error: structured error handling with `ApiMutationError` — 409 (Duplicate), 422 (Validation), other (Submission Failed)
- Removed old `setProfiles` call (profiles list still uses local mock state per instructions)

### 4. Updated Submit Button (lines 905-917)
- Added `disabled={createIndKYCMutation.loading || ...}` to prevent double-submit
- Added Loader2 spinner: `{createIndKYCMutation.loading && <Loader2 className="w-4 h-4 animate-spin" />}`
- Button text shows "Submitting..." when loading, otherwise calls `submitLabel()`

### Verified
- Lint check passes clean (zero errors, zero warnings)
- Dev server stable, no compilation errors
- Hook returns `{ mutate, loading, error, errorObj }` — used `mutate` (not `mutateAsync`) per actual hook implementation
