# Task 2.4 + 2.5 — SAR Builder + Chat Routes Refactorer

## Summary
Refactored 5 AI route files to be jurisdiction-aware, eliminating all hardcoded UAE/CBUAE references and replacing them with dynamic jurisdiction-specific parameters from the centralized Prompt Factory and Regulatory Thresholds modules.

## Files Modified

### 1. `src/app/api/ai/draft-sar/route.ts` (Action 2.4)
- Added `getRegulatoryThresholds` import
- Added `calculateSARDeadline()` function (business-day logic for Bahrain)
- Extracted jurisdiction from `alert.jurisdiction`
- All 5 SAR narrative sections now use `thresholds.*` instead of hardcoded UAE values
- SAR case creation uses calculated deadline (business/calendar days per jurisdiction)
- Audit log includes jurisdiction-specific details
- Response includes `jurisdiction`, `fiu`, `filingFormat` fields

### 2. `src/app/api/ai/chat/route.ts` (Action 2.5)
- Added `getSystemPrompt`, `PromptModule`, `getRegulatoryThresholds` imports
- Extracted jurisdiction from session via `auth.session.user.jurisdictionId`
- Replaced 3 hardcoded UAE prompts with factory-loaded jurisdiction-aware prompts
- Added module mapping (contextModule → PromptModule)
- `getFallbackResponse()` now takes optional `jurisdiction` parameter
- All fallback responses use jurisdiction-specific thresholds
- Disclaimer references correct regulator

### 3. `src/app/api/ai/enhanced/route.ts` (Action 2.5)
- Removed entire `SYSTEM_PROMPTS` constant (15 hardcoded UAE prompts)
- Added `loadSystemPrompt()` helper using Prompt Factory with 2-level fallback
- Extracted jurisdiction from session
- `getFallbackResponse()` takes optional `jurisdiction` parameter
- Audit log includes `jurisdictionId`
- Disclaimer references correct regulator

### 4. `src/app/api/ai/route.ts` (Bonus)
- Added `getRegulatoryThresholds` import
- Extracted jurisdiction from session
- Replaced: CBUAE→regulatorName, UAE→jurisdiction, me-central-1 (UAE)→me-central-1 (jurisdiction)

### 5. `src/app/api/ai/policy-rag/route.ts` (Bonus)
- Added `getSystemPrompt`, `getRegulatoryThresholds` imports
- `enhanceWithAI()` now accepts `jurisdictionId` parameter
- System prompt loaded via Prompt Factory with fallback
- Session jurisdiction extracted and passed to enhanceWithAI
- Audit logs include jurisdiction context

## Key Infrastructure Used
- `src/lib/prompts/index.ts` — `getSystemPrompt(jurisdiction, module)` → jurisdiction-aware prompts
- `src/lib/regulatory/thresholds.ts` — `getRegulatoryThresholds(jurisdiction)` → 6 GCC thresholds
- `src/lib/auth-guard.ts` — Returns `session.user.jurisdictionId` from JWT token

## Lint Results
- 0 FAILs, 21 pre-existing WARNs (unrelated audit-log warnings)
- Dev server healthy, all API routes compiling
